!!! Strict
%html{html_attrs}
  
  %head
    %title
      == ShouldBet.com - #{h(yield(:title) || AppConfig[:title])}
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    %meta{:description => "friendly bets on twitter"}/
    %meta{:keywords => "friendly bets, twitter"}/
    = javascript_include_tag :defaults, :cache => true
    = stylesheet_link_tag 'application'
    = yield(:head)
  
  %body
    #container
      #navigation
        = render( :partial => 'shared/navigation' ) if logged_in?
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"
      
      - if show_title?
        %h1=h yield(:title)
      
      ~ yield
      
      #footer.align-center.pad-top
        = link_to 'terms of use', '/terms.html'
    = render :partial => 'shared/uservoice'
